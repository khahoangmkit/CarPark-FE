export interface TripDto{
  id: number;
  bookedTicketNumber: number,
  carType: string,
  departureDate: string,
  departureTime: string,
  destination: string,
  driver: string,
  maximumOnlineOnlineTicketNumber: string
}
